project(CUniMolSys)
set(CMAKE_CXX_STANDARD 17)

file(GLOB subfolders LIST_DIRECTORIES true *)
#message("${subfolders}")
foreach(folder IN LISTS subfolders)
    if(IS_DIRECTORY ${folder})
        message("Add testing cases in ${folder}")
        add_subdirectory(${folder})
    endif()
endforeach()
